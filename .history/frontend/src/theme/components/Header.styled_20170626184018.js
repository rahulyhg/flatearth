// @flow
import styled from 'styled-components';
import mainThemeCoolor from '../con'

const Header = styled.header`
  grid-row: 1 / 1;
  grid-column: 1 / 1;
  background: 
`;

export default Header;
