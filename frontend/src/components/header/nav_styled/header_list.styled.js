import styled from 'styled-components';

const HeaderList = styled.ul`
  display:flex; 
  flex:3 auto;
  padding-left:2rem;
  list-style:none;
  margin:0;
  align-self:center;
`;

export default HeaderList;
